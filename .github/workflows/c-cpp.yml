name: Build and Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: install depencencies
      run: |
        sudo apt-get install cppcheck neofetch
        neofetch --stdout
    - name: make
      run: make
    - name: make test
      run: make test
    - name: make install
      run: sudo make install
